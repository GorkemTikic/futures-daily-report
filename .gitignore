# dependencies
node_modules/

# secrets / local env
.env
.env.local
*.secret

# OS cruft
Thumbs.db
.DS_Store

# Cloudflare Worker local state
analytics-worker/.wrangler/
analytics-worker/node_modules/

# scratch
*.tmp

# new-pipeline intermediate data packs
reports/*/datapack.json

# logs (kept on disk, NOT published — an API error string could carry a key in a URL)
logs/
logs/ALERTS.log

# rolling operational state (public URLs, but pure churn — not needed by the site)
reports/published-news.json
